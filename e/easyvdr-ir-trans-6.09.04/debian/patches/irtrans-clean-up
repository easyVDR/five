Description: <short summary of the patch>
 TODO: Put a short summary on the line above and replace this paragraph
 with a longer explanation of this change. Complete the meta-information
 with other relevant fields (see below for details). To make it easier, the
 information below has been extracted from the changelog. Adjust it or drop
 it.
 .
 easyvdr-ir-trans (6.09.04-2easyVDR8~bionic) bionic; urgency=high
 .
   * build easyvdr-irtrans daemon
Author: easyvdr-Developer <easyvdr-dev@easy-vdr.de>

---
The information above should follow the Patch Tagging Guidelines, please
checkout http://dep.debian.net/deps/dep3/ to learn about the format. Here
are templates for supplementary fields that you might want to add:

Origin: <vendor|upstream|other>, <url of original patch>
Bug: <url in upstream bugtracker>
Bug-Debian: https://bugs.debian.org/<bugnumber>
Bug-Ubuntu: https://launchpad.net/bugs/<bugnumber>
Forwarded: <no|not-needed|url proving that it has been forwarded>
Reviewed-By: <name and email of someone who approved the patch>
Last-Update: 2019-02-11

--- /dev/null
+++ easyvdr-ir-trans-6.09.04/usr/lib/easyvdr/easyvdr-start/easyvdr-irtrans
@@ -0,0 +1,65 @@
+#!/bin/bash
+#V 0.1  Bleifuss & gb
+
+. /usr/lib/vdr/easyvdr-config-loader
+
+LOGGER="logger -t easyvdr-irtrans"
+LIRCD_PATH=/var/run/lirc
+TTYUSB_DEV="/dev/ttyUSB99"
+
+[ -f /usr/bin/irserver  ]                                      || { exit 0; }
+[ -f $CFG_DIR/sysconfig             ] && . $CFG_DIR/sysconfig     || { exit 0; }
+[ x"$LIRC_HW_CONF" = x"irtrans_server"   ]                             || { exit 0; }
+[ ! -d $LIRCD_PATH                ] &&  mkdir -p $LIRCD_PATH
+$LOGGER "Starting  irtrans"
+
+EXEC_CMD="/usr/bin/irserver $TTYUSB_DEV"
+
+kill_daemon ()
+{
+ PID=$(pidof -x $1)
+
+ if [ $? -eq 0 ] ;then
+   kill $PID
+ fi
+}
+
+
+case "$1" in
+        start)
+		$EXEC_CMD & 
+            ;;
+         
+        stop)
+		kill_daemon $EXEC_CMD
+            ;;
+         
+        status)
+		PID=$(pidof -x $EXEC_CMD)
+		if [ $? -eq 0 ] ;then
+		   echo "$EXEC_CMD is running"
+		fi
+            ;;
+        restart)
+		kill_daemon $EXEC_CMD
+		sleep 1
+		$EXEC_CMD &
+            ;;
+       
+        *)
+            echo $"Usage: $0 {start|stop|restart|status}"
+            exit 1
+ 
+esac
+
+sleep 0.5
+i=1
+while [ $i -le 25 ]
+ do
+  if [ -e /dev/lircd ]; then
+   ln -s /dev/lircd $LIRCD_PATH/lircd
+   break
+  fi
+ sleep 0.2
+ i=$(( $i + 1 ))
+done
