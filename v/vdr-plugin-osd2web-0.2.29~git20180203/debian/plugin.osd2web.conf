#
# Command line parameters for vdr-plugin-osd2web
#
# For more details see:
#   - /usr/share/doc/vdr-plugin-osd2web/README
#
#
#   -s <logosuffix>, --logosuffix=<logosuffix> channel logo suffix    (default: png)
#   -p <port>,       --port=<port>             web port    (default: 4444)
#   -l <path>,       --logopath <path>         path to channel logos    (default: <plg-conf-dir>/channellogos)
#   -L,              --logonotlower            do not search logo names in lower case
#   -i,              --logobyid                logo names by channel id instead of name
#   -e,              --epgimgpath              path to epg images    (default: /var/cache/vdr/epgimages)
#
#  http://<dein host>:4444
#  setup,conf osd2web.LogLevel = 1             (0-4, default 1)
#
#  http://<dein host>:4444/skins/default/index.html?onlyView=1   (no interaktion )

